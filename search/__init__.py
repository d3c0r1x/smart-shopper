"""Гибридное поисковое ядро (ТЗ §2).

Три независимых модуля:
  structfilter — детерминированная структурная фильтрация (цена/бренд/рейтинг);
  embeddings   — биэнкодер эмбеддингов через Ollama (семантический слой);
  rerank       — двухэтапный реранкер: семантика + лексика + структура.
"""
